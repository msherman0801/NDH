import React, { Component } from 'react'
import { connect } from 'react-redux'

class Disaster extends Component {

    render() {
        return (
            <div>
            </div>
        )
    }

}

export default connect()(Disaster)